{
  "name": "@tightarse/infra",
  "version": "0.0.0",
  "private": true,
  "bin": { "tightarse": "bin/tightarse.js" },
  "scripts": {
    "build": "tsc --build",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy --all"
  },
  "dependencies": {
    "@tightarse/schema": "*",
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.0"
  },
  "devDependencies": {
    "aws-cdk": "^2.170.0"
  }
}
