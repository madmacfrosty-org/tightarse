{
  "name": "@tightarse/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "report": "node dist/report.js",
    "test:coverage": "vitest run --coverage",
    "test:mutation": "stryker run"
  },
  "dependencies": {
    "@tightarse/api-contract": "*",
    "@tightarse/schema": "*",
    "@tightarse/dynamodb": "*",
    "@tightarse/ports": "*"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "@tightarse/fixtures": "*",
    "@tightarse/transform": "*",
    "@tightarse/schema": "*",
    "@tightarse/vitest-config": "*"
  }
}
