{
  "name": "@tightarse/transform",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "backfill": "tsx src/backfill-cli.ts",
    "build": "tsc --build",
    "compare": "tsx src/compare-cli.ts",
    "reconcile": "tsx src/reconcile-cli.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:mutation": "stryker run"
  },
  "dependencies": {
    "@tightarse/metrics": "*",
    "@tightarse/schema": "*",
    "@tightarse/dynamodb": "*",
    "@tightarse/ports": "*",
    "@tightarse/aws": "*"
  },
  "devDependencies": {
    "@tightarse/vitest-config": "*",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0"
  }
}
