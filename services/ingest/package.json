{
  "name": "@tightarse/ingest",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:mutation": "stryker run"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@tightarse/aws": "*",
    "@tightarse/metrics": "*",
    "@tightarse/ports": "*",
    "@tightarse/schema": "*",
    "@tightarse/transform": "*",
    "@tightarse/truelayer": "*"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "@tightarse/vitest-config": "*"
  }
}
