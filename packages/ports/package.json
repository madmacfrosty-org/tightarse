{
  "name": "@tightarse/ports",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@tightarse/schema": "*"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "@tightarse/vitest-config": "*"
  }
}
