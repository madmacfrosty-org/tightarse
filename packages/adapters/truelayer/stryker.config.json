{
  "$schema": "../../node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "testRunner": "vitest",
  "coverageAnalysis": "perTest",
  "mutate": [
    "src/**/*.ts",
    "!src/**/*-cli.ts",
    "!src/run.ts"
  ],
  "reporters": [
    "clear-text",
    "json"
  ],
  "jsonReporter": {
    "fileName": ".stryker/mutation.json"
  },
  "incremental": true,
  "incrementalFile": ".stryker/incremental.json",
  "thresholds": {
    "high": 90,
    "low": 70,
    "break": 61.6
  },
  "tempDirName": ".stryker-tmp",
  "vitest": {
    "related": false
  },
  "_note": "Lowered when this package absorbed the provider mapper, the object transform, the backfill and the generators (#128). The old floor described a bank client and nothing else. Raise it again as coverage lands; do not lower it to pass a build."
}
