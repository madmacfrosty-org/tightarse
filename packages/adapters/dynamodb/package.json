{
  "name": "@tightarse/dynamodb",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "create-test-table": "tsx src/create-test-table.ts",
    "access": "tsx src/access.ts",
    "test:coverage": "vitest run --coverage",
    "test:mutation": "node -e \"if(!process.env.LEDGER_TEST_TABLE){console.error('Refusing to run mutation without LEDGER_TEST_TABLE: the integration tests would skip, every mutant they cover would be recorded as survived, and Strykers incremental cache would carry that into later runs as a false failure.');process.exit(1)}\" && stryker run"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@tightarse/domain": "*"
  },
  "devDependencies": {
    "@tightarse/vitest-config": "*",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0"
  },
  "type": "module"
}
