{
  "name": "@tightarse/schedule",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:mutation": "stryker run"
  },
  "dependencies": {
    "@tightarse/domain": "*",
    "@tightarse/dynamodb": "*",
    "@tightarse/aws": "*",
    "@tightarse/metrics": "*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@tightarse/vitest-config": "*",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0"
  }
}
