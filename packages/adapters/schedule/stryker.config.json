{
  "$schema": "../../node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "testRunner": "vitest",
  "coverageAnalysis": "perTest",
  "mutate": [
    "src/**/*.ts",
    "!src/**/*-cli.ts",
    "!src/run.ts"
  ],
  "reporters": [
    "clear-text",
    "json"
  ],
  "jsonReporter": {
    "fileName": ".stryker/mutation.json"
  },
  "incremental": true,
  "incrementalFile": ".stryker/incremental.json",
  "thresholds": {
    "high": 90,
    "low": 70,
    "break": 64.7
  },
  "tempDirName": ".stryker-tmp",
  "vitest": {
    "related": false
  },
  "_note": "Lowered when `reconcile-job` moved to the domain (ADR 2), taking its tests with it and leaving this package as handlers alone. The old floor described code that is no longer here. Raise it again as coverage lands; do not lower it to pass a build."
}
