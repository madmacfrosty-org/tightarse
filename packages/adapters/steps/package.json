{
  "name": "@tightarse/steps",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:mutation": "stryker run"
  },
  "dependencies": {
    "@tightarse/aws": "*",
    "@tightarse/domain": "*",
    "@tightarse/metrics": "*",
    "@tightarse/truelayer": "*"
  },
  "devDependencies": {
    "@tightarse/vitest-config": "*",
    "vitest": "^2.1.0"
  }
}
