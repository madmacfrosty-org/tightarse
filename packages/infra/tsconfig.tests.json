{
  "//": "Typechecks the infra tests under infra's own settings: it relaxes exactOptionalPropertyTypes for CDK, and needs esnext modules for import.meta in the harness.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "composite": false,
    "declaration": false,
    "declarationMap": false,
    "module": "esnext",
    "moduleResolution": "bundler",
    "types": ["node"]
  },
  "include": ["test/**/*.ts", "lib/**/*.ts", "bin/**/*.ts"]
}
