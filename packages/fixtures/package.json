{
  "name": "@tightarse/fixtures",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc --build", "test": "vitest run" },
  "devDependencies": { "vitest": "^2.1.0" }
}
