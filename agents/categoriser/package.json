{
  "name": "@tightarse/categoriser",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "run-batch": "node dist/run-cli.js",
    "rules": "tsx src/rules-cli.ts",
    "test:coverage": "vitest run --coverage",
    "test:mutation": "stryker run",
    "seed": "tsx src/seed-cli.ts",
    "categorise": "tsx src/categorise-cli.ts"
  },
  "dependencies": {
    "@tightarse/domain": "*",
    "@tightarse/dynamodb": "*",
    "@tightarse/metrics": "*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@tightarse/vitest-config": "*",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0"
  }
}
