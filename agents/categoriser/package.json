{
  "name": "@tightarse/categoriser",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "run-batch": "node dist/run.js",
    "rules": "tsx src/rules-cli.ts",
    "test:coverage": "vitest run --coverage",
    "test:mutation": "stryker run"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.700.0",
    "@tightarse/ledger": "*",
    "@tightarse/schema": "*",
    "zod": "^3.24.0",
    "@tightarse/metrics": "*"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "tsx": "^4.19.0",
    "@tightarse/vitest-config": "*"
  }
}
